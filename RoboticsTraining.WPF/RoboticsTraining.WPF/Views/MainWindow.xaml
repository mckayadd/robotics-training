<Window x:Class="RoboticsTraining.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RoboticsTraining.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Robot Name: " FontSize="16"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Position: " FontSize="16"/>
        <Button Grid.Row="2" Grid.Column="0" Content="Move Robot" Width="100"
                HorizontalAlignment="Left" Margin="0,10,0,0"
                Command="{Binding MoveRobotCommand}"
                />

        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding RobotName}" FontSize="16"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding RobotPosition}" FontSize="16"/>

    </Grid>
</Window>
